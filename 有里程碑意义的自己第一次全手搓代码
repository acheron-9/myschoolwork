用一维动态数组实现矩阵转置，从用户获取一个 n×m的矩阵，将这个矩阵转置后输出屏幕。
要求：创建n×m个元素的一维动态数组，来实现上述二维数组的转置操作。
提示：第i行第j列的元素与首元素的距离为i*n+j，序号从0开始计算。

输入说明
用户首先输入行数n和列数m，然后分n行，每行输入m个整数，例如：
3 2
7 8
5 6
3 2

输出说明
转置后输出：
7 5 3
8 6 2



MY ANSWER:
#include<iostream>
using namespace std;



int main(){

    int n,m;
    cin >> n >> m;

    int *p=new int[n*m];
    int *q=new int[n*m];
    for(int i=0;i<n;i++){   //实际上不需要两个循环，看看自己的逻辑吧
        for(int j=0;j<m;j++){
            cin >> p[i*m+j];
        }
        
    }

    for(int i=0;i<n;i++){  
        for(int j=0;j<m;j++){
            q[j*n+i] = p[i*m+j];
        
        }
        
        
    }

    for(int i=0;i<m;i++){   
        for(int j=0;j<n;j++){
            if(j<n-1)cout << q[i*n+j] <<" ";
            else cout << q[i*n+j];
        }
        cout << endl;
    }

delete []p;
delete []q;
    



return 0;
}
