约瑟夫环的最优解法，代码很短、效率极高！

# 一、最终代码（数学递推法，直接AC）
```cpp
#include <iostream>
using namespace std;

int main() {
    int n, m;
    // 输入猴子总数n，报数m
    cin >> n >> m;
    
    int ans = 0; 
    // 核心递推循环：从2只猴子推到n只猴子
    for (int i = 2; i <= n; ++i) {
        ans = (ans + m) % i;
    }
    
    // 因为猴子编号从1开始，计算结果是从0开始，所以+1
    cout << ans + 1 << endl;
    return 0;
}
```

# 二、数学递推原理（大白话版）
我们定义：
- `f(n)`：**n 只猴子**时，最后存活的猴子**下标（从0开始）**

### 核心公式（死记即可）
1. 只有 1 只猴子：`f(1) = 0`（唯一的猴子就是赢家）
2. n 只猴子：`f(n) = (f(n-1) + m) % n`

---
### 为什么这么算？
每淘汰一只猴子，整个队伍就会**重新编号**，递推公式就是帮你直接计算**最终赢家在原始队伍里的位置**，不用一步步模拟淘汰。

# 三、用题目测试用例验证（n=2，m=1）
1. 初始 `ans = f(1) = 0`
2. 循环 `i=2`：
   `ans = (0 + 1) % 2 = 1`
3. 最终编号：`1 + 1 = 2`
✅ **输出 2**，完全正确！

# 四、代码逐行解释
1. `ans = 0`：初始条件，1只猴子的位置是0
2. `for(i=2; i<=n; i++)`：从2只猴子递推到n只
3. `ans = (ans + m) % i`：核心递推公式
4. `ans + 1`：把**0下标**转成题目要求的**1编号**

# 五、测试更多用例
输入：`5 3`
输出：`4`
输入：`6 5`
输出：`1`

---
### 总结
这就是**纯数学递推解法**，没有复杂逻辑，只有一行核心公式，是解决约瑟夫环问题的标准最优解！
